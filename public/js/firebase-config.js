/**
 * Firebase Web SDK configuration for project amrit-dash-portfolio.
 * Web API keys are safe to expose in client apps; restrict with Security Rules.
 * Run `npx firebase apps:sdkconfig web` after login to refresh if needed.
 */
export const firebaseConfig = {
  apiKey: 'AIzaSyD_placeholder_replace_on_deploy',
  authDomain: 'amrit-dash-portfolio.firebaseapp.com',
  projectId: 'amrit-dash-portfolio',
  storageBucket: 'amrit-dash-portfolio.firebasestorage.app',
  messagingSenderId: '',
  appId: ''
};

export const FIRESTORE_DOC = 'portfolio/content';
export const ADMIN_EMAIL = 'amrit.dash60@gmail.com';
